import { MajPopoverProps } from "./MajPopover.types";

import "./MajPopover.scss";

const MajPopover = (props: MajPopoverProps) => {
    return <> {props?.message} MajPopover works!</>;
};

export default MajPopover;
