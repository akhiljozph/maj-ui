import { MajSliderProps } from "./MajSlider.types";

import "./MajSlider.scss";

const MajSlider: React.FC<MajSliderProps> = (props: MajSliderProps) => {
    return <>maj-slider works!</>;
};

export default MajSlider;
